{
  "name": "chats-react",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "homepage": "",
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build && serve -s build -l 4000",
    "start": "node backend/server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
